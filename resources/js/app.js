import './bootstrap';
import './meta-pixel.js';
import Alpine from 'alpinejs';

// Initialize Alpine.js
window.Alpine = Alpine;
Alpine.start();

// React components will be loaded dynamically where needed
